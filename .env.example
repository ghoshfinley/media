# User and group IDs for file ownership inside containers
# Run `id` in your terminal — use the uid and gid values
PUID=1000
PGID=1000

# Timezone
TZ=Australia/Sydney

# --- ProtonVPN / WireGuard ---
# Go to proton.me/vpn → Downloads → WireGuard configuration
# Select GNU/Linux, enable Moderate NAT and NAT-PMP, then click Create
# Copy the PrivateKey value from the [Interface] section below

WIREGUARD_PRIVATE_KEY=your_wireguard_private_key_here

# WireGuard address assigned by ProtonVPN (from Address field in the config, IPv4 only)
WIREGUARD_ADDRESSES=10.2.0.2/32

# Country for server selection — must match a ProtonVPN server country name
# Options: Netherlands, Switzerland, United States, United Kingdom, Germany,
#          France, Sweden, Canada, Australia, Japan, Singapore, Norway, Denmark
VPN_SERVER_COUNTRIES=Netherlands

# --- Homepage dashboard API keys ---
# Gluetun: pick any random string — set once here and it's used in both gluetun and Homepage
GLUETUN_API_KEY=your_random_string_here

# Jellyfin: Admin Dashboard → API Keys → +
JELLYFIN_API_KEY=your_jellyfin_api_key_here
# Seerr: Settings → General → API Key
SEERR_API_KEY=your_seerr_api_key_here
# qBittorrent: the password you set in WebUI settings
QBITTORRENT_PASSWORD=your_qbittorrent_password_here
# Sonarr/Radarr/Prowlarr: Settings → General → Security → API Key
SONARR_API_KEY=your_sonarr_api_key_here
RADARR_API_KEY=your_radarr_api_key_here
PROWLARR_API_KEY=your_prowlarr_api_key_here
# Portainer: Settings → Access Tokens → Add access token
PORTAINER_API_KEY=your_portainer_api_key_here

# Cloudflare Tunnel token — from Zero Trust Dashboard → Networks → Tunnels
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here
